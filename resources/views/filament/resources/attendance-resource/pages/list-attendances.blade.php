{{--
    Custom ListAttendances view — the page wrapper renders the header widgets
    automatically (Daily Register, Flagged Students). We intentionally omit the
    data table and the footer widgets block. Ad-hoc row edits remain reachable
    via /admin/attendances/{id}/edit or via /admin/mark-attendance.
--}}
<x-filament-panels::page>
</x-filament-panels::page>
